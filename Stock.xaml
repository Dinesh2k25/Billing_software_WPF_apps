<Window x:Class="BS.Stock"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BS"
        mc:Ignorable="d"
        Title="Stock"
        Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20,0,20,0" Height="450">
        <StackPanel VerticalAlignment="Top" MaxHeight="450" MinHeight="370">
            <StackPanel.Background>
                <ImageBrush TileMode="FlipX"/>
            </StackPanel.Background>
            <TextBlock x:Name="txtDate" Text="Stock update"
                       FontSize="18" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10" Foreground="#FF793939"/>
            <TextBox x:Name="txtID"  Margin="0,5,0,5" Height="30" VerticalContentAlignment="Center" ToolTip="Goods ID"/>

            <TextBox x:Name="txtName"  Margin="0,5,0,5" Height="30" VerticalContentAlignment="Center" ToolTip="Good's Name"/>
            <DataGrid x:Name="products"
AutoGenerateColumns="False"
CanUserAddRows="False"
Margin="0,20,0,0"
ColumnWidth="*"
SelectionMode="Single"
SelectionUnit="FullRow" MaxHeight="200" MinHeight="150">

                <DataGrid.Columns>
                      <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="*"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>

                   

                    <!-- ✅ Delete Button Column -->
                    <DataGridTemplateColumn Header="Action" Width="100" MaxWidth="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete"
                  Click="DeleteButton_Click"
                  Background="Tomato"
                  Foreground="White"
                  Padding="4,2"
                  Tag="{Binding ID}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button x:Name="ADD" Content="ADD" Width="100" Click="add_click" Margin="5,5,5,5" FontFamily="Arial" FontWeight="Bold" ToolTip="Add to stock">
                    <Button.Effect>
                        <DropShadowEffect/>
                    </Button.Effect>
                </Button>
                <Button x:Name="Update" Content="Update" Width="100" Margin="5" Click="Update_click" FontFamily="Arial" FontWeight="Bold" ToolTip="Update to the DB">
                    <Button.Effect>
                        <DropShadowEffect/>
                    </Button.Effect>
                </Button>
                <Button x:Name="Close" Content="Cancel" Width="100" Margin="5" Click="CancelButton_Click" FontFamily="Arial" FontWeight="SemiBold">
                    <Button.Effect>
                        <DropShadowEffect/>
                    </Button.Effect>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
